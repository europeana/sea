#import "../fragments/imageFields.graphql"

query StoriesListing(
  $locale: String!
  $preview: Boolean = false
  $limit: Int!
  $skip: Int = 0
  $excludeSysId: String = ""
  $categoriesFilter: [StoryFilter]
  $query: String = ""
) {
  storyCollection(
    preview: $preview
    locale: $locale
    order: datePublished_DESC
    limit: $limit
    skip: $skip
    where: {
      AND: [
        { sys: { id_not: $excludeSysId } }
        { name_contains: $query }
        { AND: $categoriesFilter }
      ]
    }
  ) {
    total
    ...storyCollectionFields
  }
}

fragment storyCollectionFields on StoryCollection {
  items {
    __typename
    sys {
      id
    }
    identifier
    name
    primaryImageOfPage {
      image {
        ...imageFields
      }
    }
  }
}
