query ProjectPageListingMinimal(
  $locale: String!
  $preview: Boolean = false
  $query: String = ""
  $excludeSysId: String = ""
) {
  projectPageCollection(
    preview: $preview
    locale: $locale
    order: datePublished_DESC
    limit: 1500
    where: { name_contains: $query, sys: { id_not: $excludeSysId } }
  ) {
    items {
      __typename
      sys {
        id
      }
      cats: categoriesCollection(limit: 5) {
        items {
          id: identifier
        }
      }
      project {
        startDate
      }
    }
  }
}
